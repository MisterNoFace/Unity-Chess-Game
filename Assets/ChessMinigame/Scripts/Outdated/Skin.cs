using Unity.VisualScripting;
using UnityEngine;



/*
public class Skin : MonoBehaviour
{
    protected Mesh SkinMesh;
    protected Material SkinMaterial;
    public Rarity SkinRarity { get; private set; } = Rarity.None;
    public Vector3 Size { get; private set; }
    public Vector3 Center { get; private set; }
    private void Awake()
    {
        SkinMesh = gameObject.GetComponent<Mesh>();
        SkinMaterial = gameObject.GetComponent<Material>();
        Size = SkinMesh.bounds.size;
        Center = SkinMesh.bounds.center;
    }

    void Start()
    {
        
    }

    void Update()
    {
    }

    public void SetRarity(Rarity rarity)
    {
        if (SkinRarity == Rarity.None)
        {
            SkinRarity = rarity;
            Debug.Log("skin has rarity: " +  rarity.ToString());
        }
    }
    public void PlayAnim(Animation anim)
    {

    }
}
*/